/// back tracking is brute force solution and not an optimisation 

// backtracking is like DPS 
//branch and bound is BFS
function valid_paranthesis(str){
    let n =str.length-1;
    let mapping = {"(":")", "[":"]", "{": "}"};
  function helper(str,i,j){
      if(i ===l){
          return 
      }


}
}